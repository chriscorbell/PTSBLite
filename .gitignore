agent/
# AGENTS.md is committed: it is project knowledge, not personal setup.
CLAUDE.md
# Planning documents are scratch. Decisions that outlive them become ADRs, and
# questions only the client can answer become issues labelled `question`.
docs/plans/
node_modules/
dist/
out/
release/
.DS_Store
*.tsbuildinfo
dist-lite/
